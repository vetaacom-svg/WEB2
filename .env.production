# Sur le domaine déployé (Vercel) : même origine + rewrites vercel.json → compatible Brave.
# Sur localhost en build prod, le code bascule automatiquement sur l’URL Supabase directe.
VITE_SUPABASE_SAME_ORIGIN=true

